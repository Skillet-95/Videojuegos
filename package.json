{
  "name": "video-games-on-rawg-react-app-express-server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.3",
    "oneyplays-api": "^1.2.0",
    "request": "^2.88.0"
  },
  "scripts": {
    "start": "npm i && cd client && npm start",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "engines": {
    "node": "12.x"
  }
}
